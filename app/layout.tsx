export const metadata = { title: 'Dogfood Chat 0502', description: 'Real-time chat app' }
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return <html lang="en"><body>{children}</body></html>
}
